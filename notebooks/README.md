# Notebooks

- Ridge Regression (linear baseline)
- Random Forest Regressor (tree-based baseline)
- HistGradientBoostingRegressor (boosted trees, strong tabular model)
- Deep Learning MLP (neural network)
